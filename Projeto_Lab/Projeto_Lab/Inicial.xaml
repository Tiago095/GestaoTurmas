<UserControl x:Class="Projeto_Lab.Inicial"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="#2B2D31" Loaded="UserControl_Loaded">

    <Grid Margin="40">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- Coluna da esquerda: Cartões -->
        <StackPanel Grid.Column="0" VerticalAlignment="Top">
            <!-- Cartão: Alunos -->
            <Border CornerRadius="20" Background="#23272A" Margin="0,0,0,20" Padding="20">
                <StackPanel>
                    <TextBlock Text="Número de Alunos" Foreground="White" FontWeight="SemiBold" FontSize="18"/>
                    <TextBlock x:Name="txtAlunos" Text="0" Foreground="#FF7289DA" FontSize="30" FontWeight="Bold" Margin="0,5,0,0"/>
                    <TextBlock Text="Total de alunos registados." Foreground="Gray" FontSize="12"/>
                </StackPanel>
            </Border>

            <!-- Cartão: Tarefas -->
            <Border CornerRadius="20" Background="#23272A" Margin="0,0,0,20" Padding="20">
                <StackPanel>
                    <TextBlock Text="Número de Tarefas" Foreground="White" FontWeight="SemiBold" FontSize="18"/>
                    <TextBlock x:Name="txtTarefas" Text="0" Foreground="#FF7289DA" FontSize="30" FontWeight="Bold" Margin="0,5,0,0"/>
                    <TextBlock Text="Tarefas atribuídas no sistema." Foreground="Gray" FontSize="12"/>
                </StackPanel>
            </Border>

            <!-- Cartão: Grupos -->
            <Border CornerRadius="20" Background="#23272A" Margin="0,0,0,20" Padding="20">
                <StackPanel>
                    <TextBlock Text="Número de Grupos" Foreground="White" FontWeight="SemiBold" FontSize="18"/>
                    <TextBlock x:Name="txtGrupos" Text="0" Foreground="#FF7289DA" FontSize="30" FontWeight="Bold" Margin="0,5,0,0"/>
                    <TextBlock Text="Grupos criados no sistema." Foreground="Gray" FontSize="12"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Coluna da direita: ListView com fundo arredondado -->
        <Border Grid.Column="2" CornerRadius="20" Background="#23272A" Padding="20">
            <StackPanel>
                <TextBlock Text="Atividades Recentes" Foreground="White" FontWeight="SemiBold" FontSize="18" Margin="0,0,0,15"/>

                <ListView x:Name="lv_Atividades" 
                          Background="Transparent" 
                          BorderThickness="0" 
                          Height="500"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Margin" Value="0,0,0,10"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <Border Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="10"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#36393F"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter Property="Background" Value="#FF7289DA"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#2F3136" CornerRadius="8" Padding="12" Margin="0">
                                <StackPanel>
                                    <TextBlock Text="{Binding Titulo}" Foreground="White" FontWeight="SemiBold" FontSize="14"/>
                                    <TextBlock Text="{Binding Descricao}" Foreground="Gray" FontSize="12" Margin="0,2,0,0"/>
                                    <TextBlock Text="{Binding Data}" Foreground="#FF7289DA" FontSize="10" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Border>
    </Grid>

</UserControl>